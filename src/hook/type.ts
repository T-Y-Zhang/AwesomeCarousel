export interface IInitCarouselValue {
  pageSize: number;
  loop: boolean;
  pagenation: boolean;
  aspectRatio: number;
}

export interface ICarouselControlValue {
  containerListProp: any;
  listItemStyle: any;
  prevButtonProp: any;
  nextButtonProp: any;
}
